# 数据库配置
DATABASE_URL="postgresql://username:password@localhost:5432/my_fullstack_app"

# JWT 配置
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-here"
JWT_ISSUER="my-fullstack-app"
JWT_AUDIENCE="api"
ACCESS_TOKEN_TTL=1800  # 30分钟
REFRESH_TOKEN_TTL=604800  # 7天

# NextAuth 配置
NEXTAUTH_SECRET="your-nextauth-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# API 配置
API_SECRET_KEY="your-api-secret-key"

# 数据加密（AES-GCM，16/24/32字节原始密钥，需以 base64url 编码）
# 生成示例：node -e "console.log(require('crypto').randomBytes(32).toString('base64').replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,''))"
DATA_ENCRYPTION_KEY="6MtTCD3v4_rladjXFdBREdaW4O6T248JR7Dr3Wu4wY4"

# CORS 配置（生产环境）
ALLOWED_ORIGINS="https://yourdomain.com,https://admin.yourdomain.com"

# 密码加密配置
BCRYPT_COST=12

# 环境
NODE_ENV="development"

# 文件上传配置 - 腾讯云硬盘存储
# 远程服务器配置
REMOTE_SERVER_HOST="118.195.188.242"
REMOTE_SERVER_PORT=22
REMOTE_SERVER_USERNAME="your_username"
REMOTE_SERVER_PASSWORD="your_password"
# 或者使用私钥文件
# REMOTE_SERVER_PRIVATE_KEY_PATH="/path/to/private/key"

# 远程服务器存储路径
REMOTE_UPLOAD_PATH="/www/wwwroot/uploads"

# 文件访问基础URL
UPLOAD_PATH="http://118.195.188.242"

# 本地临时存储路径（可选）
LOCAL_TEMP_PATH="./temp"

# 文件访问API路径
FILE_BASE_URL="/api/files"

# 文件上传限制
MAX_FILE_SIZE=10485760  # 10MB
MAX_FILES_PER_USER=100